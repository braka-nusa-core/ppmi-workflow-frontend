/**
 * DEPRECATED — Shipment now fetches live data via lib/api/shipment.ts
 * (GET /shipments, GET /shipments/:id). This fixture is no longer
 * imported anywhere (confirmed) and is left as an empty module rather
 * than deleted, matching how other legacy mock fixtures in this folder
 * are handled once their module goes live.
 */
export {}